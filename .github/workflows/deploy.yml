# ⚠️ WORKFLOW DESACTIVADO - Usar Auto-Deploy de Railway (Recomendado)
# 
# Este workflow está desactivado porque es más simple usar Auto-Deploy de Railway.
# 
# Para activar Auto-Deploy:
# 1. Ve a Railway Dashboard
# 2. Settings → Connect GitHub Repo
# 3. Selecciona BytGaming-Back
# 4. Activa Auto-Deploy
# 
# Railway desplegará automáticamente en cada push a main/master
# 
# Si necesitas usar este workflow, descomenta las líneas y configura RAILWAY_TOKEN
# Ver SOLUCION_TOKEN.md para más información

# name: Deploy to Railway
# 
# on:
#   push:
#     branches: [ main, master ]
#   workflow_dispatch:
# 
# jobs:
#   deploy:
#     name: Deploy to Railway
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           
#       - name: Install Railway CLI
#         run: npm install -g @railway/cli
#         
#       - name: Verify Railway Token
#         run: |
#           if [ -z "${{ secrets.RAILWAY_TOKEN }}" ]; then
#             echo "❌ Error: RAILWAY_TOKEN no está configurado"
#             echo "💡 Recomendación: Usa Auto-Deploy de Railway"
#             echo "📖 Ver README_DEPLOYMENT.md"
#             exit 1
#           fi
#           
#       - name: Deploy to Railway
#         run: railway up
#         env:
#           RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
